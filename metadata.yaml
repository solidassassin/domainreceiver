type: domain

status:
  class: receiver
  stability:
    development: [metrics]
  codeowners:
    active: [solidassassin]

attributes:
  domain.name:
    description: The domain name to check expiration for
    type: string

metrics:
  domain.expiry.time:
    enabled: true
    description: Unix timestamp of when the domain expires
    stability:
      level: development
    unit: s
    gauge:
      value_type: int
    attributes: [domain.name]

